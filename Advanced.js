// .call() - it is a built-in function in JS that allows calling a function with a specified context and arguments

// function greet(greeting) {
//     console.log(`${greeting}, my name is ${this.name}`);
// }

// const person = {
//     name: "vishal"
// };
// greet.call(person, "hello");

// 🔷 Event Delegation
// Event Delegation is a technique where you attach a single event listener to a parent element, instead of adding listeners to each child.

// NON -not as number  it is special value in js that represents computaional result that invalid numbmer

// parseInt("heloo");

// isNaN(NaN)

// arrow function-it shortest syntax for writing the function expression in js
// const functionName = (parameter)=>{
// }

// Regular function
// function add(a, b) {
//     return a + b;
// }
// Arrow function
// const add = (a, b) => a + b;
// console.log(add(2, 3)); // Output: 5


// React Context API lets you share values (state, functions, etc.) between components without passing props manually at every level
// theme toggleing(light/dark)
// global setting

// React.createContext()-> create AudioContext
// wrap part of your app and provide data

// H0C(higher order components);
// it is a function that takes a compoenent as input and return new compoenent
// for code reuse,handling cross-cutting concerns and loading spinners

// select <classic Dropdown>predefined options
// user must pick one of the options
// not typing allowed

// {/* <datalist></datalist> */} auto-suggestion input
// allow user to type and choose 
// good for search bar
// ux fells like google search option


{
    /* <meter> Tag – Represents a known value within a known range
    Used for things like:
    Score indicators
    Battery level
    Signal strength */
} {
    /* <progress> Tag – Represents progress of a task
    Used for:
    File uploads
    Loading indicators
    Form completion progress */
}


console.log("A"-1);/*Nan*/
console.log("tilak"+100);
console.log('2'+2+'2');